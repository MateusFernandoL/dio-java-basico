public class Operadores {
    public static void main(String[] args) throws Exception {
        // = Simbolo de igualdade utilizado para definir valor inicial ou sobreescrever
        // Cada tipo de item deve ser atribuido de uma forma
        String nome = "Mateus";
        int idade = 22;
        double peso = 69.6;
        char sexo = 'M';
        boolean doadorOrgao = true;
        // Date dataNascimento = new Date(); // Criando um objeto

        // Operadores aritméticos

        // + (Adição), - (Subtração), * (Multiplicação) e / (Divisão)

        double soma = 10.5 + 10.5;
        int subtração = 113 - 225;
        int multiplicacao = 10 * 20;
        int divisao = 15 / 3;
        int modulo = 18 % 3; // Se o resultado dessa divisao resultara em restos/
        double resultado = (10 * 7) + (20 / 4);

    }
}
